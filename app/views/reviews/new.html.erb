<h1>Create a new review</h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.select(:rating, options_for_select([0, 1, 2, 3, 4, 5]))%>
  <br>
  <br>
  <%= f.submit "add a review", class: "btn btn-primary" %>
<% end %>
